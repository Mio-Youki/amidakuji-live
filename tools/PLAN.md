# 工具路线图（tools/PLAN）

> 独立于游戏本体的 ROADMAP。按"低成本高确定性 → 体验升级 → 规模化"推进。

## ✅ 已交付（v1.0~v1.6，见 CHANGELOG.md）

像素化管线（缩放/减色/去背景）、场景画布拖拽装配、滚动/闪烁/脉动、自包含导出（保留于装配保存，导出 UI 已移除）、场景层预览（home-scene 叠底）与尺寸跟随、**打开/保存 js（页面内修改闭环）**、**home-scene.js 参数化**（元素参数 + `images` 素材层 + `show` 显示条件 + z 图层 + 多帧）、**PS 式图层列表**（收起/展开/拖拽排序/内部滚动）、**时间轴**（点击/拖动跳转 + show 时段色块 + 超采样清晰渲染）、**多帧素材（sprite sheet）**、**v1.4 界面重构**（全局设置上移顶部工具栏单行带字段名 / 播放键靠右 / 右侧纯图层栏占满页面 / 保存 js 移至图层栏右上角 / 素材参数卡片展开中文说明表单 / 画布点击自动定位卡片 / 卡片隐藏·删除按钮（z 与箭头移左）/ 删导出区 / 时间轴通用化 + 保留 SCENE 划分）、**v1.5 参数轨道 + 场景内建**（常量↔按场景数组参数模型 + `val()` 统一取值、场景栏增删改名 / 时间轴边界拖拽 / loop 联动 / 数组自动同步、按场景数组逐场景编辑 + [S]/[常量] 按钮、SCENE_SPEC.md 生成规范 + 打开 js 规范自检、拖拽重排同步卡片顺序）、**v1.6 跟随时间轴 + 单值编辑 + 显示时间弹窗**（图层栏随播放头场景显示单值、每行小 [s] 开关（关=常量/开=单场景槽，导入数组自动激活）、show 统一为按场景窗口数组 `[null|[[f0,f1],…]]`（场景内 0~1 比例，可多段）+ legacy 自动迁移、卡片头 [S] 逐场景显隐 + **场景内多段窗口条**（PS 渐变条式：加段/移动/缩放/删除，显示秒数存比例）、场景 chip 点击=切编辑场景+跳播放头、[隐藏] 独立调试层（不碰 scene 可见性，导出写 hidden:true）、暂停态编辑即时生效（refreshStage）、SCENE_SPEC v2 常量优先生成）。

## P1 —— 第二版（体验升级）

| 项 | 说明 | 预估 |
|---|---|---|
| GIF / 序列帧解码 | 直接导入 GIF 文件逐帧（渲染层已兼容 frames/fps，只需解码环节） | 1~2 天 |
| 关键帧动画 | 透明度/大小/位移随时间曲线（keyframes），替换简单 blink/pulse（含 home-scene 动态效果参数化） | 1~2 天 |
| 时间轴增强 | 关键帧编辑、素材时段拖拽调整（时间轴上直接拉长/缩短色块） | 1 天 |
| 导出增强 | 可选的场景层快照背景、代码体积压缩、单文件打包 | 半天 |

## P2 —— 中期（规模化）

| 项 | 说明 |
|---|---|
| 素材管理 | 项目文件保存/加载（JSON）、素材库分类、撤销/重做 |
| 更多色板与滤镜 | NES/GB 变体、故障风/CRT 效果、色相偏移 |
| 交互元素 | 点击/悬停反馈（页面落地动画的交互层） |
| 独立分发 | 工具自身打包为可分发单页（可选独立仓库/站点） |
