# 语音/吹气广播（Voice Public）技术文档

> 状态：已上线，残余底噪在可接受范围 ｜ 更新：2026-08 ｜ 对应功能：画线阶段的 🎤 语音 / 💨 吹气 实时广播

---

## 1. 概述

### 1.1 功能
当前画线玩家按住 🎤 或 💨 时，**房间内其他玩家能实时听到他的哼唱/吹气声**（约 0.1~0.5s 延迟）。这是"搞怪画线"体验的一部分：让命运不只是被画出来，还能**被听到**。

### 1.2 为什么选择"路线 A：PCM 中继"
备选方案评估：

| 路线 | 方案 | 结论 |
|---|---|---|
| **A. PCM 中继**（已采用） | WebAudio 捕获 → 8kHz 单声道 float32 → 独立 Socket.IO 通道广播 → 队列播放 | 全浏览器通用、实现简单、延迟可接受（~0.2s），适合小规模 |
| B. Opus 编码 | WebCodecs AudioEncoder('opus')，带宽减半 | Safari 的 Opus 支持不完整，需 PCM 降级回退；带宽本就够用，暂不需要 |
| C. WebRTC Mesh | DataChannel 直连，延迟最低 | 需 STUN/TURN 打洞、信令复杂、N 路发送；对聚会小游戏过度设计 |

**关键成本前提**：画线是**回合制**，任意时刻只有当前玩家的麦克风是开的 → "全员语音"降级为"单主播广播"，带宽成本低一个数量级（8kHz 单声道 ≈ 31kbps/听众）。

---

## 2. 架构

```
说话者（当前画线玩家）                    服务端                             听众（房间其他人）
getUserMedia ─► AudioWorklet 采集 ─────► /audio 命名空间 ─────────────► 队列(自适应缓冲+PLC)
    ↓ 42ms/块                              ↓ 校验：仅当前玩家+画线阶段         ↓ ScriptProcessor 拉取
DSP 噪声抑制链 ─► 8k 降采样 ──► 50ms 块 ──► 限速/块大小校验 ──► 广播(排除发送者) ──► 单极平滑 ──► 播放
（带 startSample 时间戳）
```

### 2.1 模块
| 模块 | 文件 | 职责 |
|---|---|---|
| 采集 | `public/voice.js` + `public/worklet-capture.js` | AudioWorklet 实时取原始音频（42ms/块），ScriptProcessor 回退 |
| DSP | `public/voice.js` | 噪声抑制链（高通→四阶低通→软门限）+ 降采样 + 音高检测 |
| 发送 | `public/app.js` | 攒 50ms 块、带时间戳、经 `audioSocket` 发送 |
| 中继 | `server.js`（`io.of('/audio')`） | 绑定/校验/限速/广播（排除发送者） |
| 接收播放 | `public/voice.js` | VoIP 式队列播放：自适应抖动缓冲 + PLC + 平滑 |

---

## 3. 数据处理链路

### 3.1 发送端
```
麦克风 (48kHz)
  → getUserMedia({ echoCancellation, noiseSuppression, autoGainControl })  // 浏览器原生降噪（Chrome/安卓）
  → AudioWorklet 'capture-processor'（2048 采样≈42ms/块；节点输出接 0 增益 sink 强制运行）
  → DSP 噪声抑制链（按 48kHz 计算系数，状态跨块连续）：
      ① 90Hz 二阶高通   —— 去直流/低频嗡声
      ② 3.2kHz 四阶低通  —— 去嘶声 + 降采样前抗混叠（两个二阶级联 = 24dB/octave）
      ③ 软门限          —— 安静时增益平滑压到 6%（快关 2.7ms/慢开 54ms），不硬切
  → 48k→8k 降采样（一阶累加盒式滤波）
  → 攒 400 采样（50ms）为一块，附 startSample 时间戳发送
```
- **无静音门限**：按住按钮本身就是"要说话"的意图；整块丢弃会制造播放缺口（曾因此卡顿，见问题 5）
- 时间戳（startSample）用于接收端按原始时间轴排程

### 3.2 传输（服务端 /audio 命名空间）
- **独立命名空间**：音频包不阻塞游戏事件
- **权威校验**：仅"当前画线玩家 + 画线阶段"可发声；限速 ~50 包/秒；块大小 64~4096 字节
- **广播排除发送者**：天然防回声（说话者设备不播放自己的流）
- 透传 `startSample` 时间戳
- 玩家退出时清理其音频绑定（`cleanupAudioBinding`）

### 3.3 接收端（VoIP 式队列播放）
```
音频块到达 → 入队（新流间隔>1.5s 则清队重缓冲）
  → 队列上限 0.5s：超出丢最旧（防延迟漂移）
  → ScriptProcessor 按音频时钟拉取（每 ~6 个输出采样消费 1 个 8k 采样）：
      ├─ 流启动：缓冲 ≥ 目标时长(自适应 0.1~0.5s，初始 0.25s) 才开始播放
      ├─ 欠载：PLC——重复最近 3ms 波形 + 衰减包络（最长隐藏 40ms），恢复时 24 采样淡入
      └─ 输出：单极低通平滑（~3.5kHz）去上采样台阶
  → 每 20 块自适应：欠载增多 → 目标 +0.05s（上限 0.5s）；稳定 → -0.02s（下限 0.1s）
无 ScriptProcessor 时回退为时间戳逐块排程
```

---

## 4. 关键设计决策

| 决策 | 理由 |
|---|---|
| 独立 `/audio` 命名空间 | Socket.IO 无消息优先级，音频与游戏事件混发会互相拖延 |
| 仅当前玩家可发声（服务端校验） | 回合制天然单说话者；防滥用 + 防回声 |
| 广播排除发送者 | 无回声反馈回路，省去 AEC |
| 8kHz 单声道 PCM | 哼唱音域 <4kHz，8k 采样足够；31kbps 带宽可忽略 |
| 发送端不做硬静音门限 | 硬切块 = 播放缺口 = 卡顿（见问题 5） |
| 时间戳排程 + 队列播放 | 丢块不压缩时间轴；音频线程拉取消除主线程排程空隙 |
| PLC + 自适应缓冲 | VoIP 核心手法：小抖动靠合成波形"糊"过去，大抖动靠缓冲吸收 |
| 原生降噪约束 + 自研 DSP 双轨 | Chrome/安卓吃原生红利；iOS 不吃约束，由 DSP 兜底 |

---

## 5. 问题与解决方案记录（时间线）

### P1：公网无声音（cloudflared + 双设备 + 游戏同步正常，发送端音高在跳）
- **症状**：发送端音高检测正常（采集 OK），但中继无声；电脑端同样如此
- **排查**：先误判为 iOS ScriptProcessorNode 缺陷（`onaudioprocess` 不触发）→ 改用 AudioWorklet + 0 增益输出 sink 强制运行 + ScriptProcessor 回退；并加诊断（"已发 N 块"计数、🔊 收听徽标、大厅"测试麦克风"自测按钮）
- **真根因**：`voice.js` 外层 API 只绑定了方法，**状态属性（onRelayChunk/relayMode/ctx 等）留在内层对象**——app.js 写在外层、采集回调读内层 → 永远 null，**任何设备都不触发采集回调**（解释了"电脑端也 0 采样"）
- **解决**：外层 API 增加 `Object.defineProperty` 状态属性代理（getter/setter 同步到内层）+ 单元测试锁定
- **教训**：方法绑定暴露 + 状态属性暴露必须成对；"自测流畅 vs 线上异常"的差异点是定位利器

### P2：点击退出时服务器崩溃
- **症状**：用户点 [退出]（leave_room）→ `TypeError: Cannot read properties of undefined (reading 'get')`
- **根因**：`cleanupAudioBinding` 中 `audioIo.sockets.sockets.get(sid)` API 笔误——`/audio` 命名空间的 `.sockets` **本身就是** Map（只有主命名空间才是 `io.sockets.sockets`）；且 e2e 未覆盖"绑定音频后退出"路径
- **解决**：改为 `audioIo.sockets.get(sid)` + 防御性判空 + e2e 回归场景（先 bind 音频再 leave）
- **教训**：新代码路径必须配套回归测试，尤其"用户真实操作才会触发的分支"

### P3：断续卡顿（第一轮）
- **症状**：能听到但断续
- **根因**：①静音门限整块丢弃安静片段 → 接收端"收到就接续"造成**时间压缩**（语速变快）②80ms 抖动缓冲太小 ③门限 0.012 偏严切掉弱音
- **解决**：块带 `startSample` 时间戳 → 接收端按原始时间轴排程（丢块只留静音缺口、不压缩）；缓冲 80→150ms；门限 0.012→0.009

### P4：持续卡顿（第二轮）
- **症状**：仍持续卡顿；**大厅自测回放流畅**（关键线索）
- **根因**：自测不走静音门限、实时中继走 → 门限仍会随机丢块 → 时间戳修复后表现为**静音缺口**
- **解决**：**彻底移除门限**（按住即意图，静音也发，带宽可忽略）；采集块 4096→2048（42ms，到达更平滑）；缓冲 150→200ms

### P5：机关枪音效（队列播放重写后）
- **症状**：哒哒哒机关枪声
- **根因**：**采样率消费错误**——输出缓冲 48kHz 却每输出 1 采样消费 1 个 8k 采样 → 播放 6× 快 → 队列瞬间见底 → PLC 反复爆发
- **解决**：分数位置 `playPos8 += 8000/输出采样率`，每约 6 个输出采样才消费 1 个 8k 采样（最近邻上采样）
- **教训**：跨采样率处理（8k→48k 播放）最容易犯的错，必须按比例消费

### P6：强底噪（手机更明显）
- **症状**：断续解决后有明显背景噪声，iPhone 更甚
- **根因**：手机麦克风增益高；iOS 不支持 `noiseSuppression` 约束，无原生降噪
- **解决**：①getUserMedia 显式开启 NS/EC/AGC（Chrome/安卓吃红利）②发送端 DSP 链：90Hz 高通 + 3.5kHz 低通 + 软门限（安静时压到 6%，快关慢开不硬切）③DSP 单测（低通衰减/直流移除/门限行为）

### P7：高频残余底噪（iPhone）
- **症状**：仍有余噪，以高频为主
- **根因**：二阶低通（12dB/octave）对 4-8kHz 衰减不足 → **降采样混叠**（>4kHz 折叠回 0-4k 可听频段）；接收端 8k→48k 最近邻上采样产生高频台阶
- **解决**：发送端低通升**四阶 3.2kHz**（24dB/octave，混叠大幅抑制）+ 接收端输出**单极平滑**（~3.5kHz）
- **结果**：残余降到可接受范围，暂保留现状

---

## 6. 测试与验证

| 层 | 测试 | 覆盖 |
|---|---|---|
| 算法单测 | `test/pitch.js` | 音高检测（6 频点 ±0.5Hz，含次谐波修复）；降采样（48k→8k 音高保持、块大小）；Voice API 代理；DSP（低通/高通/直流阻塞/软门限） |
| 端到端 | `test/e2e.js` 场景七 | 仅当前玩家可发声、PCM 广播、时间戳透传、发送者不收自己、非当前玩家/非画线阶段被拒 |
| 回归 | `test/e2e.js` 场景八 | 绑定音频后退出不崩溃 |
| 诊断工具 | 大厅「🎤 测试麦克风」 | 权限→采集→回放→广播通道 四层自检，自动探测并记住可用中继方式（worklet/script） |

运行：`npm test`（pitch + e2e 全绿）

---

## 7. 已知限制

1. **延迟**：起始 0.25s，网络差时自适应到 0.5s——"听个响、看落笔"够用，非音画同步级别
2. **音质**：8kHz 单声道窄带 + 3.2kHz 截断——人声"闷"，哼唱/吹气场景可接受
3. **麦克风权限**：需 HTTPS 或 localhost（局域网 IP 的 http 不可用，公网隧道 https 可用）
4. **TCP 传输**：丢包表现为延迟抖动（已由缓冲+PLC 吸收）；不适用 FEC
5. **iOS**：无原生降噪（DSP 兜底）、ScriptProcessor 采集缺陷（已走 AudioWorklet）
6. **单说话者**：仅当前画线玩家可发声，无多人同时发言

---

## 8. 未来可优化架构与方向

### 8.1 短期（同架构内调优）
- **参数微调**：低通截止 3.2k↔3.5k（闷 vs 亮）、门限/floor、PLC 周期与时长、初始缓冲
- **接收端谱降噪**：对播放前信号做简单频域门限（8k FFT），进一步去残余嘶声
- **队列播放改 AudioWorklet**：接收端也用 AudioWorklet + SharedArrayBuffer 环形缓冲（需 COOP/COEP 响应头），播放彻底脱离主线程调度抖动
- **VAD（语音活动检测）**：仅检测到语音时发送，静音不发（配合时间戳，不产生缺口），进一步省带宽

### 8.2 中期（质量/带宽）
- **路线 B：Opus**（WebCodecs AudioEncoder，16-24kbps + DTX），Safari 用 PCM 降级双轨；带宽减半、音质更好、可加 FEC 前向纠错
- **RNNoise 噪声抑制**（wasm 移植，~10% CPU）：AI 级降噪，替代手搓 DSP
- **时钟漂移补偿**：长时间连续画线时发送/接收采样钟漂移的补偿（重锚定 + 温和拉回）

### 8.3 远期（体验/规模）
- **路线 C：WebRTC 单向 Mesh**：DataChannel 直发听众，延迟 <100ms、不占服务器带宽；需 STUN/TURN 打洞 + 信令
- **SFU（如 mediasoup）**：多人同时发言 / 大规模房间时的合流转发，服务器出口从 O(听众) 降为 O(路数)
- **回声消除（AEC）**：若未来加"全员自由麦"模式，需 AEC + 音量限制
- **音量/响度归一化**：不同设备麦克风增益差异大，发送端做响度均衡，避免忽大忽小

### 8.4 演进路线建议
```
当前（PCM + 队列 + PLC + DSP 降噪）─► 短期参数调优/接收端谱降噪
      │
      ├─ 需要更高质量/带宽敏感 ──► Opus + RNNoise（路线 B）
      ├─ 需要更低延迟/零服务器带宽 ──► WebRTC Mesh（路线 C）
      └─ 需要多人同时发言/规模化 ──► SFU 合流
```
