# 像素抽签（Amidakuji Live）产品需求文档（PRD）

> 版本：v0.8 ｜ 状态：功能基本完备，进入沉淀维护期 ｜ 更新：2026-08
> 配套文档：`GAME_RULES.md`（规则总纲）· `CHANGELOG.md`（更新日志）· `ROADMAP.md`（需求池）· `voicepublic.md`（语音技术）

---

## 1. 产品概述

### 1.1 一句话定义
一个**在线多人实时参与**的「鬼脚图 / 阿弥陀籤（Ghost Leg / Amidakuji）」抽签游戏——用充满仪式感、参与感和欺骗性的**画线过程**替代冰冷的随机数生成，让"命运是被画出来的，而不是被掷出来的"。

### 1.2 背景与动机
- 随机数生成器虽然公平，但**没有参与感和仪式感**，不适合聚会/活动/课堂场景
- 日韩流行的"阿弥陀籤"把抽签过程变成集体创作：**每个人亲手画出决定命运的一笔**
- 本产品将这一玩法搬到线上，支持异地远程参与，同时用"像素化消隐"等机制保证结果不可提前推演

### 1.3 目标用户与场景
| 场景 | 说明 |
|---|---|
| 线上远程聚会 | 各自手机加入同一房间，实时同步 |
| 线下聚会/活动 | 一台大屏投影 + 各自手机，或传手机轮流操作 |
| 课堂/团建 | 决定分组、任务分配、表演顺序 |
| 单人模拟 | 一个人自画自抽，模拟抽签过程（活动前彩排） |

### 1.4 核心价值
1. **参与感**：每个参与者亲手画线，命运由集体绘制
2. **仪式感**：轮流落笔 → 选点/投票 → 全员同时走线揭晓，音效与动画营造悬念
3. **公平性**：数学上保证结果互不相同且完全由画线决定（置换性质）
4. **娱乐性**：语音/吹气/倾斜等搞怪画法让过程充满欢笑

---

## 2. 术语表

| 术语 | 含义 |
|---|---|
| 竖线 | N 条等距竖线，对应 N 个结果，顶部为出发点 |
| 横线（一笔） | 连接两条**相邻**竖线的水平线段，由参与者绘制 |
| 起点 | 顶部 N 个格子之一，标记从该处出发 |
| 结果 | 底部 N 个结果选项之一（完全自定义文本） |
| 置换（Permutation） | 任意合法画法都构成起点→结果的双射：每个起点唯一对应一个结果，每个结果恰好被一个起点命中 |
| 消隐 | 画线/选点阶段线条随机隐现，防止根据连线推演结果 |

---

## 3. 游戏规则与机制

### 3.1 核心规则（阿弥陀籤）
1. 画 N 条有序竖线，底部对应 N 个结果（N=2~12，完全自定义）
2. 参与者**轮流**在相邻竖线之间、**从上到下逐层**画横线（每层一笔）
3. 发起者（房主）随时决定画线结束
4. 按模式选择起点/投票后，标记从起点下行，**遇横线拐弯到相邻竖线**，到底部即为结果
5. **数学性质**：任何画法构成完美置换 → 起点不同则结果必不同，公平且可预期

### 3.2 游戏流程（六个阶段）

```
① 开房 ── 房主设置结果文本、选择最终选择模式
② 入座 ── 朋友输房间码或点分享链接加入（最多 12 人）
③ 画线 ── 按入座顺序轮流落笔，红点层逐层下移；每人 20s 倒计时，超时自动落笔；
          房主随时「结束画线」；单人可一次输入连续画线
④ 选点 ── 按模式进行（各自选择 / 房主选择 / 投票选择），起点选满或全员锁定即揭晓
⑤ 揭晓 ── 标记从起点同时下行、拐弯停顿、落定高亮；全员动画播完后才出结果页
⑥ 结果 ── 个人结果表 或 全员共享结果（含投票统计），可「再来一局」或「修改结果重开」
```

### 3.3 三种最终选择模式（开房时房主选定，开局前可改）

| 模式 | 规则 | 结果形态 | 揭晓动画 |
|---|---|---|---|
| **各自选择** | 每人各选一个起点（**不可重复，已选起点以玩家颜色描边可见**） | N 人各得各的结果 | 多标记同时走线 |
| **房主选择** | 仅房主可选起点 | 全员共享一个结果 | 单标记走线 |
| **投票选择** | 每人投一票给某个起点（可重复）；**投票过程透明**（节点上方显示各投票者色块） | 得票最多者胜出；**房主票权重 1.5**（平票时房主所投起点优先）；**归票动画**（色块→递增数字→放大→形变为起点）后揭晓 | 单标记（金色）走线 |

### 3.4 搞怪画线输入（画线阶段的四种花式画法）

| 画法 | 操作 | 映射 |
|---|---|---|
| 点击（默认） | 在红点层点一下落笔 | 点击位置 |
| 🎤 语音 | 按住按钮哼一段 | 平均音高（85~420Hz，自相关检测）→ 竖线对 |
| 💨 吹气 | 按住按钮对麦吹气 | 音量 → 竖线对 |
| 📱 倾斜 | 按住按钮左右倾斜手机 | 陀螺仪倾角（-45°~45°）→ 竖线对 |
| 🎲 命运 | 一键落笔 | 随机 |
| **连续画线** | **单人**或**单轮模式**下按住 🎤/💨/📱 不松手 | 每 450ms 自动落一笔，记录音高/气力/倾角变化，像在画一条音高曲线 |

### 3.6 画线轮次（大厅可切，房主决定）
| 轮次 | 规则 |
|---|---|
| **多轮** | 轮流每人一笔，红点层逐层下移，房主随时结束 |
| **单轮** | 每人配额 = ⌊最高笔画数/玩家数⌋；轮到你时可在配额内**连续画**（点击/语音/吹气/倾斜），画满配额或点「结束本回合」自动轮换，全员完成自动进选点 |

**最高笔画数**：房主在大厅下拉选择 **20 / 40 / 80**（决定画板容量与单轮配额基数）。

### 3.5 防推演机制（像素化消隐）
- **画线阶段 + 选点阶段**：已画线条以 6px 像素块随机溶解隐现（逐块入场/离场 + 整线周期性消失），无法可靠推演路径
- **揭晓/结果阶段**：所有线条永久完整显示（结果全程公开）
- 结果文本**全程公开**，不做"?"隐藏；揭晓时刻的仪式感来自走线动画 + 落定高亮 + 音效

### 3.7 揭晓动画与同步（动画播完 + 3s 公示才出结果）
- 每台设备播完自己的走线动画后上报 `reveal_finished`
- **全员落定 → 3s 公示停留 → 结果页**（简单路线节奏干脆）
- **后台标签页自动跳过动画立即上报**（不拖累全房等待）
- 兜底超时（30s）防止个别设备卡死/掉线卡局
- **速度自适应**：最长标记路径（垂直+水平+拐弯）>20s 时统一降速到 20s 内

### 3.8 反挂机与容错
- 画线阶段：20s 倒计时，超时自动落笔
- 选点/投票阶段：40s 超时自动补选/补票并揭晓
- **退出与托管**：游戏内退出 → 转为托管（轮到自动随机落笔/选点，播报注明"🤖托管"）；房主退出自动移交给真人；单人退出直接销毁房间
- 断线重连：玩家 ID 稳定，重连后自动回到房间并同步全量状态

---

## 4. 功能清单

### P0 —— 已实现（v0.1~v0.8）
- [x] 创建房间（自定义结果 2~12 项、选择模式）、房间码 + 分享链接一键加入
- [x] 轮流画线（红点层逐层下移、倒计时、超时自动落笔、房主决定结束）
- [x] **多轮/单轮模式**（单轮：每人配额连续画线 + 结束本回合）
- [x] **最高笔画数 20/40/80**（大厅下拉）
- [x] 三种最终选择模式（各自/房主/投票）
- [x] 单人开局与连续画线
- [x] 像素化消隐防推演（画线/选点阶段，结果全程公开）
- [x] **投票透明化**（投票者色块 → 归票计数动画 → 放大/形变为起点）＋ 房主票权重 1.5 破平票
- [x] 全员同时揭晓动画 + 动画完成握手 + 3s 公示停留 + 落定高亮
- [x] **揭晓速度自适应**（最长路径 >20s 自动降速）
- [x] **玩家编号 P1/P2… 与随机颜色**；每根线按绘制者着色；已选起点按玩家颜色描边
- [x] **退出与托管**（多人转托管自动随机、房主移交、单人销毁房间）
- [x] 结果页（个人表 / 全员共享）、再来一局、修改结果重开（结果栏 [+]）
- [x] 搞怪画法：语音音高 / 吹气 / 倾斜 / 命运
- [x] 语音/吹气**实时中继**：8kHz PCM 独立通道、VoIP 队列播放 + PLC + 自适应缓冲 + 发送端 DSP 降噪（详见 voicepublic.md）
- [x] chip tune 程序化音效、8-bit 像素风 + CRT 扫描线，移动端优先
- [x] 断线重连、连接状态指示灯、请求超时保护、**静态资源禁缓存**
- [x] 手机端兼容：输入框防缩放、安全区适配、触控目标 ≥44px、房间码输入防重复

### P1 —— 建议（短期，见 ROADMAP.md）
- [ ] 二维码邀请（扫码进房）
- [ ] 中文像素字体本地化（Fusion Pixel）
- [ ] 房间空闲自动清理（TTL）
- [ ] 接收端谱降噪 / 语音参数微调（见 voicepublic.md 短期方向）
- [ ] 揭晓镜头特写（结果落定慢动作/放大）
- [ ] 部署硬化（pm2/Docker/HTTPS 一键脚本）

### P2 —— 中期
- [ ] 前后端模块拆分、Vite + 可选 TypeScript、UI 单元测试
- [ ] Opus 编码（WebCodecs）+ RNNoise 降噪（见 voicepublic.md 中期方向）
- [ ] 状态层抽象（RoomStore 接口 → 未来 Redis）

### P3 —— 远期
- [ ] PWA 安装到桌面/主屏
- [ ] 主题皮肤（和风祭典 / 极简墨韵 / 霓虹 / 卡通）
- [ ] WebRTC Mesh / SFU、房间历史统计、多房间大厅
- [ ] 多人连接规模化（见架构设想）

---

## 5. 非功能需求

| 项目 | 要求 |
|---|---|
| 实时性 | 状态广播延迟 < 300ms（局域网/普通公网） |
| 并发 | 单房间 ≤ 12 人；单实例支撑数千房间（见架构设想） |
| 兼容性 | 现代 Chrome / Safari / Edge / Firefox，移动端优先；iOS 陀螺仪需授权 |
| 麦克风 | **需安全上下文**：localhost 或 HTTPS（局域网 IP / 公网 http 下语音不可用，点击/命运画法不受影响） |
| 可维护性 | 服务端权威校验全部输入；前端纯渲染 |
| 可测试性 | 核心算法纯函数化（音高检测、置换计算），e2e 覆盖完整流程 |

---

## 6. 技术实现现状

| 层 | 技术 |
|---|---|
| 服务端 | Node.js + Express（静态托管）+ Socket.IO（实时同步） |
| 状态机 | 内存态房间：lobby → drawing → picking → reveal → done |
| 前端 | 原生 JS + Canvas（画板渲染/走线动画）+ Web Audio（音效/音高检测）+ DeviceOrientation（倾斜） |
| 权威性 | 所有画线/选点/投票在服务端校验（轮次、阶段、合法性、唯一性） |
| 测试 | `npm test`：音高检测/DSP/降采样/API 代理单测 + e2e 场景（3 人局/单人局/超员/投票/房主/单轮/超时/握手/语音中继/退出托管） |

---

## 7. 已知限制与风险

1. **房间状态在内存**：单实例重启会丢失进行中的对局（可通过架构设想 Phase C 解决）
2. **语音功能受安全上下文限制**：远程使用需 HTTPS
3. **倾斜画法依赖陀螺仪**：桌面浏览器不可用（自动降级为提示）
4. **无房间清理**：闲置房间长期占用内存（P1 修复）
5. **无鉴权**：房间码即入场券，适合熟人聚会场景（P2 可加房主踢人/密码）

---

## 8. 里程碑

| 版本 | 内容 | 状态 |
|---|---|---|
| v0.1 | 可玩闭环：建房/画线/选点/揭晓/重开 + 基础像素风 | ✅ 交付 |
| v0.2 | 身份快照修复、单人开局、房主决定结束、超员观战、选点超时兜底 | ✅ 交付 |
| v0.3 | 结果公开、像素消隐、动画握手、跨局残留修复 | ✅ 交付 |
| v0.4 | 三种选择模式、玩家编号/颜色、退出托管、线条玩家着色 | ✅ 交付 |
| v0.5 | 多轮/单轮、最高笔画数、投票透明化 + 房主票 1.5、归票动画 | ✅ 交付 |
| v0.6 | 语音/吹气实时中继（PCM + VoIP 队列 + PLC + 降噪） | ✅ 交付 |
| v0.7 | 手机兼容、房间码防重复、连接韧性、缓存禁用 | ✅ 交付 |
| v0.8 | 揭晓速度自适应、归票动画增强、3s 公示、后台页跳过动画 | ✅ 交付 |
| v0.9+ | 概念层沉淀：规则总纲/更新日志/需求池；模块拆分；部署硬化 | 🚧 进行中 |
